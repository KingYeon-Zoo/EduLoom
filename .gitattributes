# Ensure shell scripts always use LF so they run in Linux containers (e.g. Docker)
*.sh text eol=lf

# SurrealDB RocksDB 文件必须按二进制处理，避免跨平台换行转换破坏数据库。
demo_bundle/surreal_data/** binary

# 本次新增的两个大型演示视频通过 Git LFS 保存；既有小视频继续由普通 Git 管理。
/演示视频.mp4 filter=lfs diff=lfs merge=lfs -text
/T2603359-蒜鸟都不容易队-【A01】基于百度飞桨和文心大模型的桌面微缩剧场多智能体演绎系统设计【百度】-项目演示视频.mp4 filter=lfs diff=lfs merge=lfs -text
