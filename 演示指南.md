# EduLoom 视频与播客生成现场演示指南

本指南适用于 `codex/demo` 分支，用于现场展示以下完整体验：

- 在“机器学习与深度学习”笔记本中与 AI 正常对话。
- 根据 AI 的推荐进入视频生成界面。
- 展示视频生成进度，约 40 秒后播放预生成视频。
- 在播客页面配置并生成播客，约 1 分 40 秒后播放预生成音频。

> 演示分支不会请求真实的视频生成或播客生成接口，因此不会触发豆包视频、播客脚本或语音合成等付费任务。

## 一、演示前准备

### 1. 切换到演示分支

```bash
cd /Users/zoo/Desktop/EduLoom
git switch codex/demo
git status --short --branch
```

预期显示：

```text
## codex/demo
```

### 2. 确认演示媒体存在

```bash
ls -lh frontend/public/demo-assets/
```

目录中应当存在：

- `梯度下降.mp4`：视频成品，约 10 秒。
- `机器学习与深度学习深度漫谈.mp3`：播客成品，约 23 分钟。

### 3. 安全启动服务

为了避免后台 Worker 处理历史队列，现场演示建议不要运行 `surreal-commands-worker`。分别启动数据库、API 和前端即可。

终端一：

```bash
cd /Users/zoo/Desktop/EduLoom
surreal start --log warn --user root --pass root \
  --bind 127.0.0.1:8000 \
  rocksdb:surreal_data/mydatabase.db
```

终端二：

```bash
cd /Users/zoo/Desktop/EduLoom
uv run --env-file .env run_api.py
```

终端三：

```bash
cd /Users/zoo/Desktop/EduLoom/frontend
npm run dev
```

浏览器打开：

```text
http://localhost:3000
```

默认管理员账号为 `admin / admin`，验证码按登录页显示填写。

### 4. 彩排前清理旧任务

如果视频页或播客页已经存在名称以本次演示为准的演示卡片，可以直接点击卡片上的“删除”清除。

如果页面状态异常，可在浏览器开发者工具 Console 中执行：

```js
localStorage.removeItem('eduloom-demo-media-v1')
location.reload()
```

## 二、推荐的完整演示流程

### 第一幕：笔记本 AI 对话

1. 进入“笔记本”。
2. 打开“机器学习与深度学习”笔记本。
3. 在对话区输入：

```text
请结合这个笔记本的资料，解释梯度下降为什么能够让神经网络逐步降低损失。
```

4. 发送后，向观众展示 AI 的思考过程：
   - 正在思考；
   - 正在检索知识库向量；
   - 正在整理资料；
   - 正在整合生成回复。
5. 等待 AI 正常回答。

建议讲解词：

> 这里不是预设答案，AI 会结合当前机器学习笔记本中的资料进行检索和回答，因此能够体现真实的知识库问答过程。

### 第二幕：让 AI 推荐生成视频

在同一个对话中继续输入：

```text
能否帮我生成一个视频，直观演示梯度下降在损失曲面上寻找最低点的过程？
```

正常情况下，AI 回答下方会出现“AI 建议生成学习资源”推荐卡片。

1. 点击推荐卡片中的“去生成”。
2. 页面自动进入“概念动画演示”工作区。
3. 视频生成弹窗会自动打开，并带入笔记本与 AI 推荐指令。

如果现场模型没有返回推荐卡片，可使用备用入口：

1. 点击左侧“创建”。
2. 选择“概念动画演示”。

或直接访问：

```text
http://localhost:3000/videos
```

然后点击右上角“生成”。

## 三、视频生成演示

### 1. 配置视频

视频生成弹窗中的名称、笔记本、资料、预设和自定义指令可以按现场需要任意填写。

推荐填写：

- 名称：`梯度下降：从山顶走向最低点`
- 自定义指令：`使用损失曲面和小球运动动画，直观展示梯度下降的迭代过程。`

这些配置只用于让界面和演示过程更自然，不会发送到真实视频生成接口，也不会改变最终播放的本地视频。

### 2. 确认生成

点击“生成”后：

1. 配置弹窗关闭。
2. 视频工作区出现“正在生成教学视频”动画。
3. 倒计时从约 `0:40` 开始。
4. 进度会依次展示：
   - 分析学习资料；
   - 编排内容结构；
   - 合成媒体成品。
5. 视频列表顶部出现“生成中”卡片。

### 3. 展示后台运行

点击动画右上角的关闭按钮。

关闭后：

- 仅隐藏动画，不取消生成任务。
- 可以点击左侧菜单访问其他页面。
- 可以刷新浏览器。
- 返回视频页后，任务会根据真实经过时间继续倒计时。

建议讲解词：

> 媒体生成通常需要一定时间，所以任务会在后台继续。用户不需要停留在当前页面，可以继续浏览笔记本或处理其他学习任务。

### 4. 播放视频成品

约 40 秒后返回“概念动画演示”页面：

1. 卡片状态自动切换为“已完成”。
2. 点击视频卡片。
3. 在弹窗中点击播放。

最终播放的是：

```text
frontend/public/demo-assets/梯度下降.mp4
```

## 四、播客生成演示

### 1. 进入播客页面

1. 点击左侧“智能播客”。
2. 点击右上角“生成”。

### 2. 配置播客

播客弹窗中的笔记本、资料、单集配置、发言人和附加指令可以任意选择或填写。

推荐填写：

- 单集名称：`机器学习与深度学习：从梯度下降到 Transformer`
- 附加指令：`采用主持人与算法专家对谈的形式，兼顾原理解释和工程实践。`

这些配置不会发送到真实播客生成接口，也不会触发大纲生成、脚本生成或语音合成。

### 3. 确认生成

点击“生成”后：

1. 配置弹窗关闭。
2. 播客页面出现“正在生成智能播客”动画。
3. 倒计时从约 `1:40` 开始。
4. 播客列表顶部出现“处理中”卡片。

### 4. 利用等待时间讲解

建议在 1 分 40 秒内介绍：

- 系统支持根据笔记本资料组织播客内容。
- 播客可以配置节目结构和多位发言人。
- 真实生产环境中会经历资料分析、大纲编排、脚本生成和语音合成。
- 用户可以关闭动画或前往其他页面，任务仍会继续。

可以点击动画右上角关闭按钮，再进入其他页面展示后台运行。

### 5. 播放播客成品

约 1 分 40 秒后返回“智能播客”页面：

1. 演示单集状态自动变为“已完成”。
2. 点击音频播放按钮。
3. 也可以点击“详情”，展示节目配置和发言人信息。

最终播放的是：

```text
frontend/public/demo-assets/机器学习与深度学习深度漫谈.mp3
```

## 五、现场时间安排建议

| 环节 | 建议时间 | 展示重点 |
| --- | ---: | --- |
| 打开机器学习笔记本 | 20 秒 | 笔记本资料和知识组织 |
| 第一次 AI 问答 | 10–20 秒 | 思考状态、知识库检索、正常回答 |
| 请求生成视频 | 10–20 秒 | AI 推荐卡片和自动跳转 |
| 视频等待 | 40 秒 | 三阶段动画、后台运行能力 |
| 视频播放 | 15 秒 | 本地视频成品 |
| 播客配置 | 20 秒 | 节目结构、发言人和附加指令 |
| 播客等待 | 1 分 40 秒 | 后台任务、多阶段生成概念 |
| 播客播放 | 20–30 秒 | 完整音频成品和详情页 |

完整流程建议预留 5–7 分钟。

## 六、重复彩排

- 再次点击同一类型的“生成”，会替换旧的演示任务并重新计时。
- 视频会重新从 40 秒开始。
- 播客会重新从 1 分 40 秒开始。
- 删除演示卡片只会清除浏览器中的演示状态，不会请求后端删除接口。
- 视频和播客任务相互独立，可以同时进行。

## 七、付费接口安全检查

该分支有两层保护：

1. 视频和播客确认按钮直接创建浏览器本地任务，不执行真实生成 mutation。
2. API 客户端还有硬阻断：即使界面逻辑被误改，也不会发出付费生成 HTTP 请求。

现场可在浏览器开发者工具的 Network 面板中确认：

- 视频生成期间不应出现 `POST /api/studio/generate`。
- 播客生成期间不应出现 `POST /api/podcasts/generate`。

报告、测验、思维导图和教学课件仍保留原有真实生成逻辑，现场演示时不要点击这些类型的最终生成按钮。

## 八、常见问题与应急方案

### AI 没有返回视频推荐卡片

使用左侧“创建 → 概念动画演示”，或直接访问 `/videos`。

### 点击生成后没有看到动画

1. 查看列表顶部是否已经出现“生成中”卡片。
2. 可能是之前关闭过同一个任务的动画；重新点击“生成”会创建新任务并重新显示动画。
3. 必要时清除 `eduloom-demo-media-v1` 本地存储后刷新。

### 视频或播客已经显示完成

删除对应的演示卡片，再重新点击生成；也可以直接再次生成，同类型任务会被替换并重新计时。

### 视频无法播放

确认文件存在：

```bash
file frontend/public/demo-assets/梯度下降.mp4
```

然后硬刷新浏览器页面。

### 播客无法播放

确认文件存在：

```bash
file frontend/public/demo-assets/机器学习与深度学习深度漫谈.mp3
```

浏览器通常禁止自动播放音频，需要手动点击播放按钮。

### 页面无法连接后端

依次确认端口：

```bash
nc -z 127.0.0.1 8000
nc -z 127.0.0.1 5055
nc -z 127.0.0.1 3000
```

三个命令都应成功。

## 九、演示结束

分别在三个启动终端中按 `Ctrl+C`，停止前端、API 和 SurrealDB。

演示分支保持为：

```text
codex/demo
```

不要在演示结束后将本分支直接合并到生产分支；它包含专门用于现场演示的本地媒体和生成替身逻辑。
