"""笔记本三：操作系统 —— 多模态来源素材。"""

NOTEBOOK = {
    "name": "操作系统",
    "description": "深入操作系统四大核心子系统：进程与线程、CPU 调度、内存管理与虚拟内存、文件系统，理解现代计算机如何高效管理硬件资源。",
}

SOURCES = [
    {
        "title": "进程、线程与并发：操作系统的执行抽象（教材精读）",
        "source_type": "text",
        "topics": ["进程", "线程", "并发", "上下文切换", "进程间通信"],
        "full_text": """# 进程、线程与并发

## 1. 进程：资源分配的基本单位

进程是程序的一次执行实例。每个进程拥有独立的地址空间、打开文件表、信号处理器等资源。进程控制块（PCB）是操作系统管理进程的数据结构，记录进程状态、程序计数器、寄存器、内存映射等。

进程有五种基本状态：新建、就绪、运行、阻塞、终止。状态转换由调度器和事件（如 I/O 完成）驱动。

## 2. 线程：CPU 调度的基本单位

线程是进程内的执行流。同一进程的多个线程共享地址空间和资源，但各自拥有独立的栈和寄存器。线程切换比进程切换轻量，因为无需切换地址空间（不刷新 TLB）。

- **用户级线程**：由用户库管理，切换快，但一个线程阻塞会阻塞整个进程。
- **内核级线程**：由操作系统管理，可真正并行，但切换有系统调用开销。
- **现代模型**：多数系统采用一对一映射（如 Linux 的 NPTL）。

## 3. 上下文切换的代价

上下文切换需要保存当前执行流的寄存器状态，加载下一个的状态。直接代价是寄存器保存/恢复，间接代价是缓存和 TLB 失效导致的性能损失。频繁切换（如过多线程争抢）会显著降低吞吐，这是「线程不是越多越好」的原因。

## 4. 并发带来的问题：竞态条件

当多个执行流访问共享数据且至少一个在写时，结果依赖于执行顺序，这就是竞态条件（race condition）。经典例子：两个线程同时对 count++，由于「读-改-写」非原子，最终结果可能丢失更新。

**临界区（critical section）**是访问共享资源的代码段，必须互斥执行。

## 5. 同步机制

- **互斥锁（Mutex）**：保证同一时刻只有一个线程进入临界区。
- **信号量（Semaphore）**：计数信号量可控制对 N 个资源的访问；二元信号量等价于锁。经典应用：生产者-消费者问题。
- **条件变量（Condition Variable）**：让线程等待某个条件成立，配合锁使用。
- **读写锁**：允许多个读者并发，写者独占。读多写少场景性能更好。

## 6. 死锁

死锁发生需同时满足四个必要条件：互斥、占有并等待、不可剥夺、循环等待。处理策略：
- **预防**：破坏四条件之一（如规定资源申请顺序破坏循环等待）。
- **避免**：银行家算法，只进入安全状态。
- **检测与恢复**：允许死锁发生，定期检测资源分配图中的环并恢复。
- **鸵鸟策略**：直接忽略（多数通用系统的现实选择）。

## 7. 进程间通信（IPC）

进程地址空间隔离，通信需要 OS 协助：管道、命名管道、消息队列、共享内存（最快，但需自行同步）、信号、套接字（可跨主机）。
""",
    },
    {
        "title": "技术博客：虚拟内存——操作系统最优雅的谎言（网页抓取）",
        "source_type": "url",
        "url": "https://example-os-blog.dev/virtual-memory-the-great-illusion",
        "topics": ["虚拟内存", "分页", "页表", "TLB", "缺页中断"],
        "full_text": """原文链接：https://example-os-blog.dev/virtual-memory-the-great-illusion
抓取时间：2026-06-18

# 虚拟内存：操作系统最优雅的谎言

每个进程都「以为」自己独占了整个内存空间，从地址 0 开始连续排布。这是个精心编织的谎言，由操作系统和 MMU 硬件共同维持。这篇文章讲清楚这个谎言是怎么实现的，以及为什么它如此重要。

## 为什么需要虚拟内存？

如果进程直接用物理地址，会有三个致命问题：进程之间没有隔离（一个进程能改写另一个的内存）、内存碎片难以管理、程序无法使用超过物理内存的空间。虚拟内存一次性解决了这三个问题。

## 核心机制：地址转换

每个进程有自己的虚拟地址空间。CPU 发出的虚拟地址经过 MMU（内存管理单元）转换为物理地址。转换的依据是页表（page table），由操作系统为每个进程维护。

内存被划分为固定大小的页（通常 4KB）。虚拟地址 = 虚拟页号 + 页内偏移。MMU 用虚拟页号查页表，得到物理页框号，拼上偏移得到物理地址。

## 多级页表：空间的妥协

64 位地址空间巨大，单级页表会占用海量内存。解决方案是多级页表（如 x86-64 的四级页表），只为实际使用的地址区域分配页表，大幅节省空间。代价是一次地址转换要多次访存。

## TLB：转换的加速器

多级页表每次转换都要多次访存，太慢。TLB（转换后备缓冲器）是 MMU 内的高速缓存，缓存最近的「虚拟页→物理页」映射。命中率通常很高（程序局部性），TLB 命中时转换近乎零成本。进程切换会刷新 TLB（除非有 ASID 标记），这是上下文切换代价的一部分。

## 缺页中断：谎言的兑现时刻

当访问的页不在物理内存中（页表项标记无效），触发缺页中断（page fault）。操作系统接管：
1. 检查地址是否合法（非法则段错误 SIGSEGV）。
2. 找一个空闲物理页框，没有就用置换算法（LRU、Clock）淘汰一页。
3. 若被淘汰页是脏页，先写回磁盘。
4. 从磁盘加载所需页，更新页表，重新执行触发指令。

这套机制让「按需调页」成为可能：程序启动时不必全部载入，用到才加载。

## 由此衍生的强大能力

- **内存映射文件 mmap**：把文件映射进地址空间，像访问内存一样读写文件。
- **写时复制 COW**：fork 时父子进程共享物理页，只有写时才复制，让 fork 极快。
- **交换空间 swap**：物理内存不足时把不活跃页换到磁盘，实现「超额分配」。

虚拟内存是操作系统抽象能力的巅峰——用一层间接，换来了隔离、灵活和扩展。
""",
    },
    {
        "title": "公开课讲座音频转写：CPU 调度算法（音频转写）",
        "source_type": "audio_transcript",
        "topics": ["CPU调度", "时间片轮转", "优先级调度", "多级反馈队列"],
        "full_text": """【音频转写 · 操作系统公开课第 6 讲 · 时长 42:08】

[00:15] 今天我们聊 CPU 调度。一台机器上有几十上百个进程，CPU 核心却只有几个，谁先运行、运行多久，由调度器决定。调度的目标其实是相互冲突的：我们既想要高吞吐量，又想要低响应时间，还要公平，不能让某个进程饿死。

[04:30] 先看最简单的，先来先服务 FCFS。按到达顺序执行，简单公平。但有个大问题叫「护航效应」——一个长作业排在前面，后面一堆短作业全得等它，平均等待时间很差。

[10:20] 改进一下，最短作业优先 SJF。优先调度运行时间最短的作业，可以证明它的平均等待时间最优。但有两个问题：一是你怎么知道一个作业要运行多久？只能预测。二是长作业可能一直被插队，饿死。

[17:45] 这些都是非抢占的。现代交互式系统需要抢占。时间片轮转 RR 是关键：每个进程分配一个时间片，比如 10 毫秒，时间到就被抢占，轮到下一个。时间片太大就退化成 FCFS，太小则上下文切换开销过大。这是个经典的权衡。

[26:10] 再讲优先级调度。给每个进程一个优先级，高优先级先跑。问题还是饿死——低优先级可能永远轮不上。解决办法是「老化 aging」：等待越久优先级越高，保证最终能执行。

[33:00] 实际系统用的是组合拳——多级反馈队列 MLFQ。设多个队列，优先级从高到低，时间片从小到大。新进程进最高优先级队列；如果用完时间片还没结束，降一级；如果主动放弃 CPU（比如等 I/O），保持或提升优先级。这样 I/O 密集型的交互进程自然待在高优先级、响应快，CPU 密集型的计算进程沉到低优先级、慢慢跑。它不需要预知运行时间，却能逼近 SJF 的效果，非常聪明。

[40:30] Linux 的 CFS 完全公平调度器又是另一套思路，用红黑树按「虚拟运行时间」排序，每次选 vruntime 最小的进程，追求的是公平。下节课讲。下课。
""",
    },
    {
        "title": "文件系统与磁盘 I/O：数据如何持久化（学习笔记）",
        "source_type": "text",
        "topics": ["文件系统", "inode", "磁盘调度", "RAID", "日志文件系统"],
        "full_text": """# 文件系统与磁盘 I/O

## 1. 文件系统的抽象

文件系统把存储设备组织成「文件」和「目录」的层次结构，向上提供统一接口（open/read/write/close），向下管理磁盘块的分配与回收。

## 2. inode：文件的元数据核心

在类 Unix 文件系统中，每个文件对应一个 inode，存储元数据：文件大小、权限、所有者、时间戳，以及数据块的指针。注意：文件名不在 inode 中，而在目录项里，目录项把文件名映射到 inode 号。这就是为什么硬链接能让多个文件名指向同一 inode。

inode 用「多级索引」组织数据块指针：直接块指针 + 一级间接 + 二级间接 + 三级间接。小文件用直接块快速访问，大文件靠间接块扩展容量。

## 3. 磁盘块分配策略

- **连续分配**：文件占连续块，顺序读快，但易产生外部碎片，文件增长困难。
- **链式分配**：块之间用指针串联，无碎片，但随机访问慢（FAT 文件系统的思路）。
- **索引分配**：用索引块集中存放所有数据块指针，兼顾随机访问与灵活增长（inode 即此思路）。

## 4. 磁盘调度算法（机械硬盘）

机械硬盘的寻道时间是性能瓶颈，调度算法旨在减少磁头移动：
- **FCFS**：先来先服务，公平但低效。
- **SSTF**：最短寻道时间优先，可能饿死远端请求。
- **SCAN（电梯算法）**：磁头单向扫到底再返回，像电梯。
- **C-SCAN**：扫到底直接回到起点，响应更均匀。

注意：SSD 没有机械寻道，这些算法对它意义不大，SSD 更关注写放大、磨损均衡和并行通道。

## 5. 崩溃一致性：日志文件系统

如果写文件途中断电，文件系统可能处于不一致状态（如已分配块但 inode 未更新）。日志文件系统（如 ext4、NTFS）借鉴数据库的 WAL：先把操作写入日志，再真正落盘。崩溃后重放日志即可恢复一致性，避免全盘扫描 fsck。

## 6. RAID：用冗余换可靠与性能

- **RAID 0**：条带化，性能翻倍，但无冗余，一块坏全完。
- **RAID 1**：镜像，可靠但容量减半。
- **RAID 5**：分布式奇偶校验，允许坏一块盘，兼顾容量与可靠。
- **RAID 6**：双校验，允许坏两块。
- **RAID 10**：先镜像再条带，高性能高可靠，成本高。

## 7. 页缓存与 I/O 优化

操作系统用页缓存（page cache）把磁盘数据缓存在内存，read 命中缓存则无需访盘，write 先写缓存再异步刷盘（write-back）。这解释了为什么第二次读同一文件快得多，也解释了为什么断电可能丢失未刷盘的数据——这是性能与持久性的经典权衡。
""",
    },
]
