"""笔记本一：数据结构与算法 —— 多模态来源素材。

每个 source 模拟一种真实的内容来源（教材章节文本 / 技术博客网页 / 讲座音频转写），
带有标题、主题标签、来源类型与正文。正文足够长，作为下游报告/PPT/视频/播客生成的素材。
"""

NOTEBOOK = {
    "name": "数据结构与算法",
    "description": "面向计算机专业核心课程的数据结构与算法学习空间，涵盖哈希表、树、图与排序算法的原理、复杂度分析与工程实践。",
}

SOURCES = [
    {
        "title": "《算法导论》第 11 章：散列表（教材精读）",
        "source_type": "text",
        "topics": ["哈希表", "散列函数", "冲突解决", "时间复杂度"],
        "full_text": """# 散列表（Hash Table）

## 1. 直接寻址表的局限

当关键字全域 U 较小时，可以用直接寻址表（direct-address table）实现动态集合：用数组 T[0..m-1] 直接以关键字为下标存储。查找、插入、删除均为 O(1)。但当全域 U 极大时，分配大小为 |U| 的数组既不现实也极度浪费——实际存储的关键字集合 K 往往远小于 U。

## 2. 散列表的核心思想

散列表通过散列函数 h: U → {0,1,...,m-1} 将关键字映射到 m 个槽位（slot）。元素 k 被存放在槽 h(k) 中。当 |K| 远小于 |U| 时，散列表所需空间为 Θ(|K|)，而平均查找时间仍可保持 O(1)。

关键问题在于：两个不同关键字可能映射到同一个槽，这称为**冲突（collision）**。由于 |U| > m，冲突无法完全避免（鸽巢原理），只能尽量减少并妥善处理。

## 3. 冲突解决之一：链接法（Chaining）

链接法将所有散列到同一槽的元素放入一个链表中。槽 j 指向链表头。
- 插入：在链表头插入，最坏 O(1)。
- 查找：遍历对应链表，时间正比于链表长度。
- 删除：若为双向链表，可在 O(1) 内完成。

**装载因子（load factor）** α = n/m，表示每个槽平均存放的元素数。在简单均匀散列假设下，一次不成功查找的期望时间为 Θ(1+α)，成功查找同样为 Θ(1+α)。当 n = O(m) 时，α = O(1)，所有字典操作平均 O(1)。

## 4. 冲突解决之二：开放寻址法（Open Addressing）

开放寻址法将所有元素直接存入散列表本身，不使用额外链表。当发生冲突时，按某个**探查序列（probe sequence）**依次检查其他槽，直到找到空槽。

常见探查方式：
- **线性探查**：h(k,i) = (h'(k) + i) mod m。简单但易产生「一次聚集（primary clustering）」。
- **二次探查**：h(k,i) = (h'(k) + c1·i + c2·i²) mod m。缓解一次聚集，但有「二次聚集」。
- **双重散列**：h(k,i) = (h1(k) + i·h2(k)) mod m。探查序列更接近随机，性能最好。

开放寻址法的装载因子 α < 1。在均匀散列假设下，一次不成功查找的期望探查次数至多为 1/(1-α)。当 α = 0.5 时约 2 次，α = 0.9 时约 10 次——这说明散列表性能在接近满载时急剧下降。

## 5. 散列函数的设计

好的散列函数应近似满足简单均匀散列：每个关键字等概率地散列到 m 个槽中任意一个。常见方法：
- **除法散列**：h(k) = k mod m。m 通常取远离 2 的幂的素数。
- **乘法散列**：h(k) = ⌊m·(k·A mod 1)⌋，A 为常数（如黄金分割比 0.618…）。
- **全域散列（universal hashing）**：从一族散列函数中随机选取，可证明期望冲突次数低，能抵御针对性的最坏输入。

## 6. 动态扩容与摊还分析

当装载因子超过阈值（如 0.75）时，散列表需要**再散列（rehash）**：分配更大的数组并重新插入所有元素，单次代价 O(n)。但通过「倍增扩容」策略，n 次插入的总代价为 O(n)，**摊还（amortized）**到每次插入仍是 O(1)。这是 Java HashMap、Python dict、Go map 等工业实现的共同基础。

## 7. 工程实践要点

- Java 8 的 HashMap 在单个桶链表长度超过 8 且表容量 ≥ 64 时，将链表转为红黑树，把最坏查找从 O(n) 降到 O(log n)，抵御哈希碰撞攻击。
- Python dict 采用开放寻址 + 随机化扰动，并保证插入顺序（3.7+）。
- 字符串哈希需注意 hash flooding 攻击：攻击者构造大量碰撞键使性能退化为 O(n²)，对策是带密钥的 SipHash。
""",
    },
    {
        "title": "技术博客：红黑树为什么是「平衡」的？（网页抓取）",
        "source_type": "url",
        "url": "https://example-tech-blog.dev/posts/red-black-tree-balance",
        "topics": ["平衡二叉树", "红黑树", "AVL树", "旋转操作"],
        "full_text": """原文链接：https://example-tech-blog.dev/posts/red-black-tree-balance
抓取时间：2026-06-20

# 红黑树为什么是「平衡」的？一次性讲透五条性质

很多同学背得出红黑树的五条性质，却答不上来「为什么这五条就能保证平衡」。这篇文章用一个核心引理把它讲清楚。

## 二叉搜索树的痛点

普通 BST 在最坏情况下会退化成链表——比如按升序插入 1,2,3,4,5，树高变成 O(n)，查找退化到 O(n)。平衡树的目标就是把树高控制在 O(log n)。

## 红黑树的五条性质

1. 每个节点是红色或黑色。
2. 根节点是黑色。
3. 每个叶子（NIL 哨兵）是黑色。
4. 红色节点的两个子节点都是黑色（即不存在两个相连的红节点）。
5. 从任一节点到其每个叶子的所有路径，包含相同数目的黑色节点（黑高 black-height 相同）。

## 核心引理：为什么这能保证平衡？

关键在性质 4 和性质 5 的配合。设某节点 x 的黑高为 bh(x)。可以归纳证明：以 x 为根的子树至少包含 2^bh(x) − 1 个内部节点。

再由性质 4，从根到叶的任意路径上，红节点不能相邻，所以红节点数不超过黑节点数，于是整条路径长度至多是黑高的 2 倍。设树高为 h，根的黑高至少 h/2，于是 n ≥ 2^(h/2) − 1，解得 h ≤ 2·log₂(n+1)。**树高被锁死在 O(log n)**——这就是平衡的本质。

## 红黑树 vs AVL 树

- AVL 树要求任意节点左右子树高差不超过 1，平衡更严格，查找略快。
- 红黑树平衡较松，但插入/删除时旋转次数更少（插入至多 2 次旋转，删除至多 3 次），写多读少场景更优。
- 工程取舍：C++ std::map、Java TreeMap、Linux 进程调度的红黑树都选了它，正是看中「修改成本低」。

## 旋转：维持平衡的原子操作

红黑树通过左旋、右旋两种 O(1) 操作配合变色来恢复性质。插入新节点默认染红（尽量不破坏黑高），再根据叔节点颜色分情况调整。理解旋转不要死记，要抓住「旋转不改变中序遍历顺序」这一不变量。
""",
    },
    {
        "title": "公开课讲座音频转写：图算法导论（Dijkstra 与最小生成树）",
        "source_type": "audio_transcript",
        "topics": ["图论", "最短路径", "Dijkstra", "最小生成树", "贪心算法"],
        "full_text": """【音频转写 · 计算机算法公开课第 9 讲 · 时长 47:32】

[00:12] 好，我们今天讲图算法。图是最通用的数据结构之一——网络、地图、社交关系、依赖关系，本质上都是图。一个图 G = (V, E)，V 是顶点集，E 是边集。

[03:40] 先说最短路径。假设你在导航，要从 A 点到 B 点找最快路线。如果所有边权非负，Dijkstra 算法就是标准答案。它的核心是贪心：维护一个已确定最短距离的顶点集合 S，每次从集合外选出距离起点最近的顶点，加入 S，然后「松弛」它的所有出边。

[09:15] 什么叫松弛 relax？对于边 (u, v)，如果 dist[u] + w(u,v) < dist[v]，就更新 dist[v]。这一步是所有最短路径算法的灵魂。Dijkstra 用优先队列（最小堆）来快速取出最近顶点，时间复杂度是 O((V+E) log V)。

[16:50] 注意 Dijkstra 的前提是边权非负。如果有负权边，它会出错，这时要用 Bellman-Ford，它能处理负权，还能检测负权环，代价是 O(VE) 慢一些。

[24:30] 接下来是最小生成树 MST。问题是：给一个连通带权无向图，找一棵连接所有顶点、总权值最小的树。两个经典贪心算法——

[26:05] Kruskal 算法：把所有边按权值从小到大排序，依次考察，如果加入这条边不形成环就加入。判断成环用并查集（Union-Find），近乎 O(1)。整体 O(E log E)。

[33:20] Prim 算法：从任意顶点出发，像 Dijkstra 一样维护一个集合，每次选出连接集合内外的最小权边。用堆实现是 O(E log V)。稠密图用 Prim，稀疏图用 Kruskal，这是经验法则。

[40:10] 这两个算法都体现了贪心法的威力：局部最优导致全局最优。但要强调，贪心不是万能的，它的正确性需要证明——MST 的正确性依赖「切割性质」：横跨任意切割的最小权边一定属于某棵最小生成树。

[45:50] 下节课我们讲网络流和二分图匹配。作业是实现 Dijkstra 和 Kruskal，并在一个一千个顶点的随机图上对比运行时间。下课。
""",
    },
    {
        "title": "排序算法复杂度速查与选择策略（学习笔记）",
        "source_type": "text",
        "topics": ["排序算法", "快速排序", "归并排序", "堆排序", "稳定性"],
        "full_text": """# 排序算法复杂度速查与选择策略

## 比较类排序总览

| 算法 | 平均时间 | 最坏时间 | 空间 | 稳定性 |
|------|---------|---------|------|--------|
| 冒泡排序 | O(n²) | O(n²) | O(1) | 稳定 |
| 插入排序 | O(n²) | O(n²) | O(1) | 稳定 |
| 选择排序 | O(n²) | O(n²) | O(1) | 不稳定 |
| 希尔排序 | O(n^1.3) | O(n²) | O(1) | 不稳定 |
| 归并排序 | O(n log n) | O(n log n) | O(n) | 稳定 |
| 快速排序 | O(n log n) | O(n²) | O(log n) | 不稳定 |
| 堆排序 | O(n log n) | O(n log n) | O(1) | 不稳定 |

## 比较排序的下界

任何基于比较的排序算法，在最坏情况下至少需要 Ω(n log n) 次比较。证明用决策树模型：n 个元素有 n! 种排列，决策树至少有 n! 个叶子，树高至少 log(n!) = Θ(n log n)。所以归并、堆排序已是渐近最优。

## 快速排序为何实际最快

快排平均 O(n log n)，最坏 O(n²)（已排序数据 + 固定取首元素为枢轴）。但它常数因子小、原地排序、缓存友好，所以工程上常胜过归并。关键优化：
- 三数取中或随机化选枢轴，避免最坏情况。
- 小数组（< 16）切换到插入排序。
- 三路划分处理大量重复元素。
这正是 C 标准库 qsort、Java 的 Dual-Pivot QuickSort 的思路。

## 线性时间排序

突破 Ω(n log n) 下界需要放弃比较模型：
- **计数排序**：适用于小范围整数，O(n+k)。
- **基数排序**：按位排序，O(d(n+k))。
- **桶排序**：数据均匀分布时 O(n)。

## 选择策略

- 数据量小：插入排序（常数小、对近乎有序数据极快）。
- 需要稳定且最坏可控：归并排序。
- 通用最快：随机化快速排序。
- 内存受限且要 O(n log n) 最坏：堆排序。
- 整数且范围有限：计数/基数排序。
- 外部排序（数据超内存）：多路归并。
""",
    },
]
