"""笔记本二：计算机网络 —— 多模态来源素材。"""

NOTEBOOK = {
    "name": "计算机网络",
    "description": "系统梳理 TCP/IP 协议栈、HTTP/HTTPS、DNS 与网络安全的核心原理，结合 RFC 文档、抓包实践与课程讲解，构建完整的网络知识体系。",
}

SOURCES = [
    {
        "title": "RFC 9293 摘读：TCP 三次握手与可靠传输（标准文档）",
        "source_type": "pdf",
        "topics": ["TCP", "三次握手", "可靠传输", "拥塞控制", "滑动窗口"],
        "full_text": """# Transmission Control Protocol (TCP) 核心机制摘读
# 基于 RFC 9293（2022 年更新，取代 RFC 793）

## 1. TCP 的设计目标

TCP 在不可靠的 IP 之上提供面向连接的、可靠的、基于字节流的传输服务。它解决四个核心问题：可靠性（丢包重传）、有序性（序号重排）、流量控制（不压垮接收方）、拥塞控制（不压垮网络）。

## 2. 三次握手（Three-Way Handshake）

建立连接需要三次握手，目的是同步双方的初始序列号（ISN）并确认彼此的收发能力：

1. 客户端发送 SYN，携带初始序号 seq=x，进入 SYN-SENT 状态。
2. 服务端回复 SYN+ACK，seq=y, ack=x+1，进入 SYN-RCVD 状态。
3. 客户端发送 ACK，seq=x+1, ack=y+1，连接建立，双方进入 ESTABLISHED。

**为什么是三次而不是两次？** 两次握手无法让服务端确认「客户端确实能收到服务端的报文」。若用两次，一个延迟的旧 SYN 到达服务端会让服务端建立一个客户端并不想要的连接，浪费资源。三次握手能让双方都确认对方的发送和接收能力。

## 3. 四次挥手（Connection Termination）

关闭连接需要四次挥手，因为 TCP 是全双工的，每个方向必须独立关闭：FIN → ACK → FIN → ACK。主动关闭方最后进入 TIME-WAIT 状态，等待 2·MSL（报文最大生存时间），确保最后的 ACK 到达，并让旧连接的残余报文在网络中消失。

## 4. 可靠传输：序号与确认

TCP 给每个字节编号。接收方通过 ACK 告知「期望收到的下一个字节序号」（累积确认）。发送方维护重传定时器，超时未收到 ACK 则重传。现代 TCP 还用 SACK（选择性确认）告知接收方已收到的不连续段，避免不必要的重传。

## 5. 流量控制：滑动窗口

接收方在 ACK 中通告自己的接收窗口 rwnd（剩余缓冲区大小）。发送方的发送量不能超过 rwnd，从而防止快速发送方淹没慢速接收方。窗口为 0 时发送方暂停，并定期发送窗口探测包。

## 6. 拥塞控制：网络的自我保护

流量控制保护接收方，拥塞控制保护整个网络。经典算法包含四个阶段：
- **慢启动**：拥塞窗口 cwnd 从 1 MSS 开始，每收到一个 ACK 翻倍（指数增长）。
- **拥塞避免**：cwnd 达到慢启动阈值 ssthresh 后转为线性增长。
- **快速重传**：收到 3 个重复 ACK 立即重传，不等超时。
- **快速恢复**：快重传后将 ssthresh 减半，cwnd 设为 ssthresh，避免回到慢启动。

现代 Linux 默认使用 CUBIC，Google 提出的 BBR 则基于带宽和 RTT 建模，在高带宽长距离链路上表现更优。
""",
    },
    {
        "title": "技术博客：一个 HTTPS 请求从输入网址到页面呈现都发生了什么（网页抓取）",
        "source_type": "url",
        "url": "https://example-net-blog.dev/what-happens-when-you-type-a-url",
        "topics": ["HTTP", "HTTPS", "TLS", "DNS", "浏览器渲染"],
        "full_text": """原文链接：https://example-net-blog.dev/what-happens-when-you-type-a-url
抓取时间：2026-06-19

# 在浏览器输入一个网址后，到底发生了什么？

这是经典面试题，也是理解整个网络栈的绝佳线索。我们以访问 https://www.example.com 为例，一步步拆解。

## 第一步：DNS 解析

浏览器先把域名变成 IP 地址。查找顺序：浏览器缓存 → 操作系统缓存 → hosts 文件 → 本地 DNS 服务器（递归解析器）。本地 DNS 若没有缓存，会从根域名服务器 → 顶级域 .com 服务器 → example.com 权威服务器，逐级查询（迭代查询），最终拿到 A 记录（IPv4）或 AAAA 记录（IPv6）。DNS 默认走 UDP 53 端口，响应过大时退化到 TCP。现代还有 DoH（DNS over HTTPS）来防止窃听和篡改。

## 第二步：建立 TCP 连接

拿到 IP 后，与服务器的 443 端口进行 TCP 三次握手，建立可靠连接。

## 第三步：TLS 握手（HTTPS 的核心）

HTTP 是明文的，HTTPS = HTTP + TLS。TLS 握手要完成三件事：
1. **协商加密套件**：双方商定使用哪种密钥交换、对称加密和哈希算法。
2. **身份认证**：服务器出示由 CA 签发的数字证书，浏览器验证证书链是否可信、域名是否匹配、是否过期。
3. **密钥协商**：通过 ECDHE 等算法在不安全信道上安全地协商出一个会话密钥。后续通信用这个对称密钥加密，兼顾安全与性能。

TLS 1.3 把握手从 2-RTT 优化到 1-RTT，并支持 0-RTT 会话恢复，显著降低延迟。

## 第四步：发送 HTTP 请求与响应

连接安全后，浏览器发送 HTTP 请求报文（请求行 + 请求头 + 请求体）。服务器返回响应（状态行 + 响应头 + 响应体）。常见状态码：200 成功、301/302 重定向、304 命中缓存、404 找不到、500 服务器错误。HTTP/2 用多路复用解决队头阻塞，HTTP/3 干脆把传输层换成基于 UDP 的 QUIC。

## 第五步：浏览器渲染

浏览器解析 HTML 构建 DOM 树，解析 CSS 构建 CSSOM，合并成渲染树，经过布局（Layout）、绘制（Paint）、合成（Composite）最终呈现页面。期间遇到 JS 会下载执行，可能阻塞渲染。

## 小结

一个简单的网址访问，串起了应用层（HTTP/DNS）、安全层（TLS）、传输层（TCP）、网络层（IP）的协同。理解这条链路，网络知识就有了骨架。
""",
    },
    {
        "title": "实验课视频转写：用 Wireshark 抓包分析 TCP 与 HTTP（视频转写）",
        "source_type": "video_transcript",
        "topics": ["抓包分析", "Wireshark", "网络调试", "TCP", "HTTP"],
        "full_text": """【实验课录屏转写 · 网络协议抓包实战 · 时长 38:15】

[00:20] 同学们好，今天这节实验课我们用 Wireshark 实际抓一次网络通信，把课本上的 TCP 三次握手、HTTP 请求响应真正「看见」。打开 Wireshark，选择你的网卡，开始捕获。

[02:45] 为了过滤噪声，我们在过滤栏输入 tcp.port == 80，只看 HTTP 流量。现在我打开浏览器访问一个测试网站，大家看抓包列表立刻刷出来一堆包。

[05:30] 来看前三个包，这就是三次握手。第一个包，Flags 字段是 [SYN]，序号 Seq=0，这是客户端发起连接。第二个包，服务器回 [SYN, ACK]，Seq=0, Ack=1。第三个包，客户端回 [ACK]，Ack=1。三次握手完成，状态变为 ESTABLISHED。课本上的理论，现在是活生生的报文。

[12:10] 握手之后紧接着就是 HTTP 请求。展开这个包，应用层是 GET / HTTP/1.1，下面是请求头：Host、User-Agent、Accept 等等。右键选 Follow → HTTP Stream，可以看到完整的请求和响应明文——注意这是 HTTP 不是 HTTPS，所以全是明文，这也说明了为什么登录密码绝不能走 HTTP。

[19:40] 我们看服务器的响应：HTTP/1.1 200 OK，响应头里有 Content-Type、Content-Length、Set-Cookie。响应体就是 HTML。

[24:55] 现在做个对比实验。我把过滤改成 tcp.port == 443，访问 HTTPS 网站。再 Follow Stream，大家看——全是乱码！因为 TLS 已经加密了。这就直观说明了 HTTPS 的价值：即使有人在链路上抓包，也看不到内容。

[31:20] 最后教一个排错技巧。如果你发现大量 [TCP Retransmission] 或 [TCP Dup ACK]，说明网络在丢包；如果看到 [TCP ZeroWindow]，说明接收方缓冲区满了，是流量控制在起作用。这些都是诊断网络问题的关键信号。

[37:00] 今天的作业：抓一次完整的 DNS 查询，找出查询类型和响应的 IP 地址，写进实验报告。下课。
""",
    },
    {
        "title": "网络安全基础：常见攻击与防御机制（学习笔记）",
        "source_type": "text",
        "topics": ["网络安全", "DDoS", "中间人攻击", "SQL注入", "对称与非对称加密"],
        "full_text": """# 网络安全基础：常见攻击与防御

## 1. 加密体系：对称与非对称

- **对称加密**（AES、ChaCha20）：加解密用同一密钥，速度快，适合加密大量数据。难点在密钥分发。
- **非对称加密**（RSA、ECC）：公钥加密、私钥解密，解决密钥分发问题，但慢。
- **混合加密**：实际系统（如 TLS）用非对称加密协商出一个对称会话密钥，再用对称加密通信，兼顾安全与效率。
- **哈希与签名**：哈希（SHA-256）保证完整性；数字签名（私钥签名、公钥验证）保证完整性 + 不可否认性 + 身份认证。

## 2. 常见网络攻击

- **DDoS（分布式拒绝服务）**：用海量请求耗尽目标资源。防御：流量清洗、CDN 分散、限流。
- **中间人攻击（MITM）**：攻击者在通信双方之间窃听或篡改。防御：TLS + 证书验证 + 证书钉扎（pinning）。
- **SQL 注入**：用户输入拼接进 SQL 被恶意利用。防御：参数化查询（预编译语句）、最小权限、输入校验。
- **XSS（跨站脚本）**：注入恶意脚本在他人浏览器执行。防御：输出转义、CSP 内容安全策略、HttpOnly Cookie。
- **CSRF（跨站请求伪造）**：诱导用户在已登录站点执行非预期操作。防御：CSRF Token、SameSite Cookie、校验 Referer。

## 3. 认证与授权

- **认证（Authentication）**：你是谁。手段：密码、多因素认证 MFA、OAuth2、生物识别。
- **授权（Authorization）**：你能做什么。模型：RBAC（基于角色）、ABAC（基于属性）。
- **JWT**：自包含的令牌，服务端无需存储会话，但要注意签名验证和过期控制。

## 4. 纵深防御原则

安全没有银弹，要分层设防：网络层防火墙/WAF、传输层 TLS、应用层输入校验与最小权限、数据层加密存储。同时遵循「默认拒绝」「最小权限」「不信任客户端输入」三条铁律。任何把安全寄托于「攻击者不知道」的做法（隐晦式安全）都是脆弱的。
""",
    },
]
