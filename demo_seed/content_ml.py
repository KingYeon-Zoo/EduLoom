"""笔记本四：机器学习与深度学习 —— 多模态来源素材。"""

NOTEBOOK = {
    "name": "机器学习与深度学习",
    "description": "从监督学习到神经网络，系统掌握机器学习的数学基础、核心算法与深度学习模型，理解梯度下降、反向传播与 Transformer 等关键机制。",
}

SOURCES = [
    {
        "title": "机器学习导论：监督学习与模型评估（教材精读）",
        "source_type": "text",
        "topics": ["监督学习", "过拟合", "偏差方差", "交叉验证", "正则化"],
        "full_text": """# 机器学习导论：监督学习与模型评估

## 1. 什么是机器学习

机器学习是让计算机从数据中自动学习规律，而非显式编程规则。形式化地：给定训练数据，学习一个函数 f: X → Y，使其在未见数据上也能做出好的预测。

三大范式：
- **监督学习**：数据带标签，学习输入到输出的映射。分类（离散输出）与回归（连续输出）。
- **无监督学习**：数据无标签，发现内在结构。聚类、降维。
- **强化学习**：智能体通过与环境交互、获得奖励来学习策略。

## 2. 监督学习的核心流程

1. 准备数据：划分训练集、验证集、测试集。
2. 选择模型：线性回归、逻辑回归、决策树、SVM、神经网络等。
3. 定义损失函数：衡量预测与真实的差距（回归用 MSE，分类用交叉熵）。
4. 优化：用梯度下降等算法最小化损失。
5. 评估：在测试集上衡量泛化能力。

## 3. 泛化、过拟合与欠拟合

机器学习的核心目标是**泛化**——在未见数据上表现好，而非记住训练数据。
- **欠拟合**：模型太简单，训练集和测试集都差。表现为高偏差。
- **过拟合**：模型太复杂，记住了训练集的噪声，训练集极好但测试集差。表现为高方差。

## 4. 偏差-方差权衡

泛化误差可分解为偏差²+方差+不可约误差。
- 偏差：模型假设与真实规律的系统性差距。
- 方差：模型对训练数据扰动的敏感度。
增加模型复杂度通常降低偏差、升高方差。最优模型在两者之间取得平衡。这是理解所有模型选择问题的根本框架。

## 5. 对抗过拟合的武器

- **更多数据**：最有效但最贵。
- **正则化**：L1（产生稀疏，可做特征选择）、L2（权重衰减，抑制大权重）。
- **交叉验证**：k 折交叉验证更可靠地估计泛化性能、调超参。
- **早停**：验证误差不再下降就停止训练。
- **Dropout / 数据增强**：深度学习中的常用手段。

## 6. 评估指标

- 分类：准确率、精确率、召回率、F1、ROC-AUC。注意类别不平衡时准确率会骗人。
- 回归：MSE、MAE、R²。
- 不要只看单一指标，要结合业务目标。比如癌症筛查更看重召回率（宁可误报不可漏报）。
""",
    },
    {
        "title": "技术博客：梯度下降与反向传播，一篇讲透神经网络如何学习（网页抓取）",
        "source_type": "url",
        "url": "https://example-ml-blog.dev/gradient-descent-and-backprop",
        "topics": ["梯度下降", "反向传播", "链式法则", "学习率", "优化器"],
        "full_text": """原文链接：https://example-ml-blog.dev/gradient-descent-and-backprop
抓取时间：2026-06-21

# 梯度下降与反向传播：神经网络到底是怎么学习的

「神经网络会学习」这句话听起来很神秘，其实背后只是两个朴素的数学工具：梯度下降负责「往哪走」，反向传播负责「高效算出往哪走」。这篇文章把它们讲透。

## 一、梯度下降：在误差曲面上下山

训练神经网络，本质是找一组参数（权重 W 和偏置 b），使损失函数 L(W) 最小。把 L 想象成一片山地，我们要走到最低谷。

梯度 ∇L 指向函数上升最快的方向，那么它的反方向就是下降最快的方向。梯度下降的更新规则就一行：

    W ← W − η·∇L(W)

其中 η 是学习率，控制每步走多远。学习率太大会在谷底反复横跳甚至发散；太小则收敛极慢。这是最关键的超参数。

## 二、三种梯度下降

- **批量梯度下降 BGD**：用全部数据算梯度，准确但慢，内存吃紧。
- **随机梯度下降 SGD**：每次用一个样本，快但噪声大、震荡。
- **小批量 Mini-batch**：折中，每次用一小批（如 32、64），是实践标准。既能利用并行，又有适度噪声帮助跳出局部最优。

## 三、反向传播：链式法则的高效应用

一个深层网络有百万级参数，怎么高效算出每个参数的梯度？答案是反向传播——它本质就是微积分的链式法则，加上动态规划的复用思想。

前向传播：输入逐层计算，最终得到预测和损失。
反向传播：从损失出发，反向逐层用链式法则计算梯度。关键在于，后一层算出的梯度可以复用给前一层，避免重复计算。这让计算复杂度从指数级降到与前向传播同阶。

直觉上，反向传播在回答：「每个权重对最终误差贡献了多少？」贡献大的权重就该多调整。

## 四、优化器的进化

朴素 SGD 有很多问题，于是有了一系列改进：
- **Momentum 动量**：累积历史梯度，像滚下山的小球带惯性，能冲过小坑、加速收敛。
- **AdaGrad / RMSProp**：自适应学习率，对频繁更新的参数减小步长。
- **Adam**：结合动量和自适应学习率，是当今最常用的默认优化器。

## 五、为什么深度学习能 work

理论上多层网络的损失曲面非凸、有无数局部最优，但实践发现：高维空间中大多数临界点是鞍点而非糟糕的局部最优，而且很多局部最优的质量都不错。加上好的初始化、批归一化、残差连接，让深层网络的训练成为可能。这就是深度学习革命的数学底座。
""",
    },
    {
        "title": "课程视频转写：从神经网络到 Transformer（视频转写）",
        "source_type": "video_transcript",
        "topics": ["神经网络", "CNN", "RNN", "注意力机制", "Transformer"],
        "full_text": """【课程录播转写 · 深度学习专题第 12 讲 · 时长 51:20】

[00:18] 这节课我们把深度学习的几大网络架构串起来，看清楚它们各自解决什么问题，最后落到现在最火的 Transformer。

[03:25] 先从最基础的全连接网络说起。每一层的每个神经元都和上一层所有神经元相连。它通用，但参数量爆炸，而且完全没利用数据的结构。处理一张图片，如果把像素全展平接全连接，参数多到无法训练。

[09:40] 于是有了卷积神经网络 CNN。核心两个思想：局部连接和参数共享。一个小小的卷积核在整张图上滑动，提取局部特征，比如边缘、纹理。多层卷积逐层抽象，从边缘到部件再到物体。CNN 极大减少参数，还自带平移不变性，是图像领域的革命。

[18:50] 但 CNN 不擅长处理序列，比如一句话、一段语音，因为它们有时间依赖。这就轮到循环神经网络 RNN。RNN 有「记忆」，隐藏状态会随时间传递，把前面的信息带到后面。可是朴素 RNN 有梯度消失问题，记不住长距离依赖。LSTM 和 GRU 用门控机制缓解了这个问题，能记住更长的上下文。

[29:15] 不过 RNN 有个硬伤——它必须按顺序一步步算，没法并行，训练慢。而且再长的记忆也会衰减。2017 年，注意力机制彻底改变了局面。

[35:40] 注意力机制的核心思想是：处理一个词时，让它直接「看」序列中所有其他词，并根据相关性分配权重。相关的词权重高，无关的低。这就是 Query、Key、Value 的内积计算。它不需要逐步传递，任意两个位置直接建立联系，还能完全并行。

[43:10] Transformer 就是把注意力机制堆叠起来的架构，完全抛弃了循环。自注意力 + 多头注意力 + 位置编码 + 前馈网络 + 残差连接，就是它的全部积木。它解决了长依赖和并行两大难题，于是有了 BERT、GPT 这些大模型。今天的大语言模型，骨架都是 Transformer。

[49:30] 总结一条主线：全连接太笨重 → CNN 抓空间结构 → RNN 抓时间序列 → Transformer 用注意力统一了一切，还能大规模并行。架构演进的背后，是「如何更好地利用数据结构、如何更高效地训练」这两个永恒的问题。下课。
""",
    },
    {
        "title": "实践笔记：用 PyTorch 训练第一个分类模型（代码笔记）",
        "source_type": "text",
        "topics": ["PyTorch", "模型训练", "数据加载", "训练循环", "实践"],
        "full_text": """# 实践笔记：用 PyTorch 训练第一个分类模型

## 1. 完整训练流程的五个部件

任何 PyTorch 训练脚本都由这五部分组成，理解了就理解了深度学习工程：
1. 数据：Dataset + DataLoader 负责加载和分批。
2. 模型：继承 nn.Module，定义 __init__（层）和 forward（前向计算）。
3. 损失函数：如 nn.CrossEntropyLoss。
4. 优化器：如 torch.optim.Adam。
5. 训练循环：前向 → 算损失 → 反向 → 更新。

## 2. 定义模型

```python
import torch.nn as nn

class MLP(nn.Module):
    def __init__(self, in_dim, hidden, n_classes):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(in_dim, hidden),
            nn.ReLU(),
            nn.Dropout(0.2),
            nn.Linear(hidden, n_classes),
        )
    def forward(self, x):
        return self.net(x)
```

## 3. 训练循环：最核心的四行

```python
for epoch in range(epochs):
    for x, y in train_loader:
        optimizer.zero_grad()      # 1. 清空上一轮梯度
        out = model(x)             # 2. 前向传播
        loss = criterion(out, y)   # 3. 计算损失
        loss.backward()            # 4. 反向传播算梯度
        optimizer.step()           # 5. 更新参数
```

记住这个节奏：清零梯度 → 前向 → 算损失 → 反向 → 更新。忘记 zero_grad() 会导致梯度累积，是新手最常见的 bug。

## 4. 训练与评估模式

- model.train()：启用 Dropout 和 BatchNorm 的训练行为。
- model.eval() + torch.no_grad()：评估时关闭 Dropout、固定 BN、不算梯度，省内存又正确。
切换模式忘记调用，会让评估结果莫名其妙地波动，这是另一个高频坑。

## 5. 常见调试经验

- 损失不下降：检查学习率（太大发散、太小停滞）、数据是否归一化、标签是否对齐。
- 训练集好测试集差：过拟合，加正则、Dropout、数据增强或早停。
- Loss 变成 NaN：学习率过大或数值不稳定（如 log(0)），加梯度裁剪或检查输入。
- GPU 显存爆掉：减小 batch size，或用梯度累积模拟大 batch。

## 6. 从能跑到跑好

先用一个小数据子集让模型「过拟合」——如果连几十个样本都学不会，说明代码有 bug。能过拟合后再加正则、调超参、扩大数据。这个「先过拟合再正则化」的策略，是调试深度学习模型的黄金法则。
""",
    },
]
