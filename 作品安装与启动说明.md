# EduLoom 作品安装与启动说明

本交付包已内置经过脱敏处理的完整演示数据。首次启动会自动安装演示数据库和媒体，不需要填写任何 API 密钥即可浏览：

- 4 个主题笔记本：数据结构与算法、计算机网络、操作系统、机器学习与深度学习
- 16 个多模态学习来源
- 报告、测验、知识导图、幻灯片、视频、播客
- 学习路径、六维能力评估、学习者画像和聊天会话

## macOS 启动

1. 首次安装运行环境：
   - Node.js 20 或更高版本
   - Python 3.11 或更高版本
   - [uv](https://docs.astral.sh/uv/)
   - [SurrealDB](https://surrealdb.com/install)
2. 双击 `启动EduLoom.command`。
3. 浏览器会自动打开 `http://localhost:3000`。
4. 默认管理员账号和密码均为 `admin`。
5. 在终端按 `Ctrl+C` 可停止全部服务。

如果 macOS 首次双击提示没有执行权限，请在本目录终端运行：

```bash
chmod +x 启动EduLoom.command run-dev.sh scripts/bootstrap-demo-data.sh
./启动EduLoom.command
```

## Windows 启动

1. 安装 Node.js 20+、Python 3.11+、uv 和 SurrealDB。
2. 双击 `启动EduLoom.bat`。
3. 浏览器访问 `http://localhost:3000`，账号和密码均为 `admin`。

## 演示数据保护机制

- 原始模板位于 `demo_bundle/`，它不含任何 API 密钥或服务商凭证。
- 仅在本地数据库不存在时复制数据库模板，不会覆盖后续新增的个人笔记本。
- 演示媒体会在每次启动时校验，避免移动文件夹或重新解压后出现资源缺失。
- 如需恢复到初始演示状态，停止服务后删除 `surreal_data/` 与 `data/`，再次启动即可。

## 在线 AI 功能

内置数据可离线浏览和播放；聊天、重新生成资源等在线 AI 功能需要在“设置”中配置自己的模型服务商密钥。比赛交付包不会分发开发者密钥。
